define('mini-core/page-view', [], function() {
  "use strict";

  return {
    getId: function () {
      return 'page-123';  // Page view ids not currently relevant in the extension (no metrics)
    }
  };
});

