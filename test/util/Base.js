// Leadfoot utility class base

define(
    [],
    function () {

        'use strict';

        class Base {
            constructor(remote) {
                this.remote = remote;
            }
        }

        return Base;
    }
);
