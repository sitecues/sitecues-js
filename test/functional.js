// This file is designed to return a list of filepaths, without extensions,
// relative to the functional test directory that you want to run as tests.

define(
    [   // dependencies...
        // 'foo/bar'
    ],
    function () {
        return [
            'simple'
        ];
    }
)
