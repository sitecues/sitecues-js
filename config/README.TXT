The files that exist in this directory are the default core config files for the JS library.
If they don't exist in the directory <PROJECT_ROOT>/source/js/.cfg, they will be automatically
copied there by 'make run'.