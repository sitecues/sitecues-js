// This is the default dev configuration which defines the host endpoints for needed services in dev envs.
(window.eqnx.__eqnx_cfg=window.eqnx__eqnx_cfg||{}).hosts = {
	up : "up.sitecues.com",
	ws : "ws.sitecues.com"
};
