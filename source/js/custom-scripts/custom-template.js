  /**
   * Customer-Customization File
   * -----------------------------------------------------------------------------------------------
   *
   * INSTRUCTIONS:
   * 
   *   This file is used to deliver a custom customer customization.
   *
   *     USAGE STEPS:
   *
   *       1 - DO NOT EDIT THIS FILE!
   *       2 - Create a Customization ID for use with your customization.
   *
   *              Your Customization-ID is created like this: 
   *
   *                  "custom_[site-id]_[JIRA-issue-number]"
   *
   *              , where site_id is a value from https://equinox.atlassian.net/wiki/display/EN/Account+and+Site+IDs
   *
   *              For example the full Customization-ID should appear like this:
   *
   *                  "custom_FFF123_EQ-1234"
   *
   *       3 - Copy this file into to the customization directory, using your Customization-ID as a file name:
   *
   *              Example: "source/js/customizations/custom_FFF123_EQ-1234.js"         
   *
   *       4 - Fill out the CUSTOMIZATION-DESCRIPTION below dashed line.
   *       6 - Follow the comments below the dotted line, making sure you fill in the blanks.
   *
   * -----------------------------------------------------------------------------------------------
   * CustomId    : custom_FFF123_EQ-1234
   * IssueLink   : https://equinox.atlassian.net/browse/[ISSUE-REFERENCE-NUMBER]
   * Description : [Write the description of what the customization does in here. Example: "This
   *               customization fixes problems with overflow:hidden elements on EEOC.gov by
   *               updating the CSS rules of some elements of the home-page."] 
   */

  // 7 - Insert your Customization-ID in BOTH places parts of the def() statement...
  sitecues.def('[Customization-ID]', function (module, callback, log) {

    // 8 - Insert your [module-to-customize] and your [required-modules] in the use() statement below...
    // 
    //     Eg:
    //     
    //     sitecues.use('customization', 'picker', 'jquery', function (customization, picker, jquery) {
    //     
    sitecues.use('custom', '[required-module-1]', '[required-module-2]', function (custom, required_module_1, required_module_2) {

      custom.register({

        // 9 - Insert the [module-to-customize] for the module that the customization should run in.
        //          You can find the [module-to-customize] name in the def() statement of the module
        //          that you want to customize. Make sure you use the full path-name of the module
        //          including any slashes.
        //          
        //          Eg:
        //          
        //          module: 'mouse-highlight/picker',
        //          
        module: '[module-to-customize]',

        // 11 - Insert the Customization-ID] here.
        customId: '[Customization-ID]',

        func: function (event) {
          
          // 12 - Insert your customization code here. This function runs AFTER the module is loaded.

        }

      });

      callback();
    })
  });

  // 13 - Delete all instruction comments from you customization script file.

  // 14 - Include a reference to your customization file in the makefile. Place the customization
  // script before the module you are customizing.