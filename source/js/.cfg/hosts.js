// This is the default dev configuration which defines the host endpoints for needed services in dev envs.
(window.sitecues.__sitecues_cfg=window.sitecues.__sitecues_cfg||{}).hosts = {
	up : "up.sitecues.com",
	ws : "ws.sitecues.com"
};
